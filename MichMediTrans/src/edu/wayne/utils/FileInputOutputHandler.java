package edu.wayne.utils;

public class FileInputOutputHandler {
	/**
	 * Enables importing files (patientdata, driver data etc.) in Json, CSV, pdf
	 * @param filename
	 * @param format
	 * @return
	 */
	public boolean importFile(String filename, String format){
		
		return true;
	}
	
	
	/**
	 * Enables exporting billing record in Json, CSV, pdf
	 * @param filename
	 * @param format
	 * @return
	 */
	public boolean exportFile(String filename, String format){
		
		return true;
	}
}
